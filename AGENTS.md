# Growth 2.0 작업 규칙

- 사용자는 개발자가 아니다. 모든 결과는 쉬운 한국어로 설명한다.
- 한 번에 기능 하나만 구현한다.
- 승인된 작업 범위를 벗어나지 않는다.
- 운영 데이터베이스에 연결하지 않는다.
- 운영 배포를 하지 않는다.
- 기존 migration 파일을 수정하지 않는다.
- 실제 개인정보를 사용하지 않는다.
- 테스트용 가짜 데이터만 사용한다.
- 새로운 외부 패키지 설치가 필요하면 작업을 멈추고 먼저 보고한다.
- 공용 컴포넌트나 기존 화면을 수정하기보다 Growth 2.0 전용 파일을 우선 사용한다.
- 관련 테스트, lint, type check를 실행한다.
- 이번 작업으로 발생한 오류만 수정한다.
- 기존의 관련 없는 오류는 수정하지 않고 보고만 한다.
- 오류 수정은 최대 3회까지만 자동으로 시도한다.
- 3회 이후에도 해결되지 않으면 임의로 구조를 바꾸지 않고 멈춘다.
- 기능 하나가 끝나면 다음 기능으로 자동 진행하지 않는다.
- 결과가 확인되기 전에는 push 또는 배포하지 않는다.
- 작업 완료 후 변경 내용을 하나의 로컬 Git 커밋으로 보관한다.
- 원격 저장소에는 push하지 않는다.
