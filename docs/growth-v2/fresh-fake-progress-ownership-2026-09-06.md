# 수동 다운로드 시험의 학습 진도 2건 소유자 확인

검사일: 2026-09-06 16:12 KST

후속 완료: 16:19 KST 확인 기준으로 아래 가짜 진도와 연결된 계정·학생·파일을 정리하고 잔여 수량 0건을 확인했다. [정리 완료 기록](fresh-manual-fixture-cleanup-2026-09-06.md) 참고. 이 문서 본문은 삭제 전에 수행한 소유자 확인 기록이다.

## 결론

남아 있던 `user_progress` 2건은 **이번 가짜 학생 계정과 가짜 관리자 계정의 자료**임을 확인했다. 두 건 모두 레벨 1·XP 0이며, 학습 상태 18개 항목이 초기 기본값과 일치한다. 경험치 지급 이력과 강좌 진도는 각각 0건이다.

이번에는 소유자 확인만 수행했다. 계정·학생·파일·진도를 삭제하거나 변경하지 않았으며, 정리가 끝났다는 뜻은 아니다. 이전의 간헐적 인증 오류 원인도 이번 성공만으로 해결됐다고 단정하지 않는다.

## 정확한 대상

- Supabase: `codingssok-growth-v2-fresh-test`, ref `opcdcuedhwyuyhzaubpu`.
- 실행 ID: `50a27b78-29d4-4479-ad16-1d36c2e467ee`.
- 학생 행 ID: `ff607de5-75e2-4156-a1f7-8e5971ddb5b0`.

| 시험용 소유자 | 현재 역할 | Auth/프로필 ID | 진도 행 ID |
| --- | --- | --- | --- |
| 가짜연결학생 | student | `6e04e06d-ba4a-475b-815d-653ae094eef1` | `3fda0088-bf0a-4bfa-b873-31e06bef32e5` |
| 가짜연결관리자 | admin | `79284547-893c-41e0-8946-fd53075865a9` | `7aa37512-0393-400d-9c5c-6fbce22368cb` |

실제 학생의 이름·학교·연락처·학습 자료는 사용하지 않았다. 위 식별자는 이번 시험용 가짜 자료에 한정한다.

## 읽기 검증 순서와 근거

1. `user_progress` 전체 수량이 2건인지 먼저 확인했다. 응답 자료는 `limit=0`으로 받지 않았다.
2. 고정된 가짜 학생 ID 1개만 조회해 이름 `가짜연결학생`, 학교 `가짜연결초`, 학년 `1학년`, Auth와 프로필 연결을 대조했다.
3. 이 학생의 `auth_user_id`와 `created_by`로 나온 두 ID의 프로필만 조회했다. 이름·역할·승인 상태 및 프로필의 XP 0·레벨 1·Iron을 확인했다.
4. 이 두 ID의 Auth 계정을 개별 GET으로 조회했다. 학생 이메일은 고정된 학생 ID를, 관리자 이메일은 고정된 실행 ID를 포함한 기존 가짜 주소와 정확히 일치했다. Auth metadata의 역할과 가짜 이름도 대조했다. 다른 Auth 계정 목록은 조회하지 않았다.
5. 확인된 두 ID로만 진도 행을 조회했다. 정확히 각 계정당 1건이며, 전체 수량 2건과도 일치했다.
6. 아래 기본값을 전부 검사하고 경험치 지급 이력·강좌 진도가 각각 0건인지 확인했다. 마지막에 전체 진도 수량을 다시 조회해 2건이 유지됨을 확인했다.

검사한 18개 초기 상태:

- `xp=0`, `level=1`, `streak=0`, `best_streak=0`, `last_active_date=null`.
- `total_problems=0`, `accuracy=0`, `avg_solve_time_minutes=0`.
- `rank=999`, `tier=Iron`, `placement_done=false`, `tier_points=0`.
- `total_code_runs=0`, `streak_data={}`, `hints_count=0`.
- `xp_boost_data={}`, `profile_effects=[]`, `gamification_synced_at=null`.

두 진도 행은 각각 해당 프로필과 생성 시각이 같았고, 진도 행의 수정 시각도 생성 시각과 같았다.

- 학생 프로필·진도: `2026-09-05T07:30:27.570127+00:00`.
- 관리자 프로필·진도: `2026-09-05T07:30:27.302591+00:00`.

이는 계정 초기화 시점의 기본 진도 자료라는 판단을 뒷받침한다. 현재 값이 기본값이라는 것은 확인했지만, 모든 과거 변경·삭제 이력을 조사한 것은 아니다.

## 관리자 계정에도 진도가 있는 점

로컬 `007_learning_progress_xp.sql`은 학생 역할 프로필에 기본 진도를 생성한다. `001_identity_access.sql`은 Auth 생성 시 전달된 역할을 참고하며, 원래의 가짜 계정 생성 도구는 생성 후 프로필 역할을 다시 지정한다.

현재 가짜 관리자 계정에 초기 진도가 존재한다는 사실과 소유자는 확인됐다. 다만 실제 생성 당시의 트리거 실행 순서와 역할 전달 시점을 재현한 것은 아니므로, **관리자가 먼저 학생으로 만들어졌다는 설명은 아직 추정**으로 둔다. 이를 확인하려고 새 계정을 만들거나 역할·기존 migration을 변경하지 않았다.

## 안전 및 검사 결과

- 일회성 읽기 전용 도구로 **GET 9회 성공**, 쓰기·삭제·로그인·RPC·파일 다운로드 요청 0회.
- 시험 호스트와 허용한 API 경로만 접근했으며, 요청별 시간 제한과 리디렉션 금지를 적용했다. Auth 조회는 검증된 가짜 학생 행에서 나온 두 ID로 제한했다.
- 실패 시 구체적 오류 코드·문구·요청 ID만 비밀값을 가리고 출력하고 중단하도록 했다. 이번 실행은 인증 오류 없이 통과했으며 자동 재시도하지 않았다.
- 기존 Secret key를 이번 프로세스에서만 사용했다. 키를 출력하거나 파일에 저장하지 않았고, 클립보드와 임시 환경변수를 정리했다. 키 생성·교체·폐기 및 프로젝트 설정 변경은 없었다.
- 대표 진도 조회 요청 ID: `01a0758f-8dc7-7fbe-b577-8ba258bcb98d`.
- 검사 종료 시각: `2026-09-06T07:12:17.924Z`.
- 관련 로컬 테스트 5개 파일 56건 통과. 대상은 fresh 학습 진도/XP 구조·검증, fresh 신원 구조, XP 엔진, 학부모 XP/레벨이다.
- 해당 테스트 5개 파일 ESLint, TypeScript(`--noEmit --incremental false`), 문서 diff 검사, 일회성 도구의 Node 문법 검사 모두 통과했다. 앱 소스 변경이 없어 빌드는 다시 실행하지 않았다.
- 앱 소스와 migration 변경 없음. 새 패키지 설치 없음. 기존 무관한 미추적 파일은 보존한다.
- 운영 DB 연결, 원격 push, PR, Preview/운영 배포 없음.

## 다음 단계

이번 진도 2건의 소유자 확인은 완료했다. 다음은 **동일 실행의 계정·학생·파일 전체를 삭제 직전에 다시 대조한 뒤, 정확한 ID와 저장 경로만 지정해 정리하는 단계**다. 이 문서는 삭제 실행 또는 정리 후 0건 확인을 대신하지 않는다. 사용자 컴퓨터에 내려받은 파일과 다른 실행의 자료는 정리 대상이 아니다.
