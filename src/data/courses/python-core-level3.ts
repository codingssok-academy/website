import type { PythonCoreSessionSpec } from "./python-core-generator";

export const PYTHON_CORE_LEVEL_3_SPECS = [
    {
        number: 25, title: "객체의 상태와 메서드를 안전하게 설계해요", subtitle: "캡슐화 관점과 상태 변경 규칙을 클래스에 담습니다.", concept: "객체 상태 관리", analogy: "은행 계좌 잔액은 아무나 직접 고치지 않고 입금·출금 창구를 통해서만 바꾸는 것과 같아요.", explanation: "객체 상태가 어떤 규칙으로 바뀌는지 메서드에 모으면 잘못된 값과 중복 코드를 줄일 수 있습니다.", syntax: ["class Account:\n    def deposit(self, amount):\n        if amount > 0:\n            self.balance += amount", "def withdraw(self, amount):\n    if 0 < amount <= self.balance:\n        self.balance -= amount"],
        predict: { code: "class Counter:\n    def __init__(self):\n        self.value = 0\n    def add(self, amount):\n        if amount > 0:\n            self.value += amount\n\nc = Counter()\nc.add(3)\nc.add(-2)\nprint(c.value)", question: "출력 결과는?", options: ["-2", "0", "1", "3"], answer: 3, explanation: "양수 3만 더해지고 -2는 조건을 통과하지 못합니다." },
        guided: { title: "안전한 점수판 클래스", question: "음수 점수는 반영하지 않는 add_score 메서드를 구현하세요.", codeTemplate: "class ScoreBoard:\n    def __init__(self):\n        self.score = 0\n\n    def add_score(self, points):\n        if points > 0:\n            self.score += points\n\nboard = ScoreBoard()\nboard.add_score(20)\nboard.add_score(-5)\nprint(board.score)", answer: "상태 변경 전에 유효한 값인지 검사합니다." },
        check: { question: "상태 변경을 메서드에 모으는 장점은?", options: ["규칙을 한 곳에서 검사", "객체를 만들 수 없음", "모든 값이 문자열", "반복문 제거"], answer: 0, explanation: "변경 규칙을 한 곳에서 일관되게 검사할 수 있습니다." },
        build: { title: "한도 있는 에너지 시스템", question: "에너지가 0보다 작거나 100보다 커지지 않게 사용·회복 메서드를 만드세요.", codeTemplate: "class Energy:\n    def __init__(self):\n        self.value = 100\n    def use(self, amount):\n        self.value = max(self.value - amount, 0)\n    def charge(self, amount):\n        self.value = min(self.value + amount, 100)\n\nenergy = Energy()\nenergy.use(35)\nenergy.charge(10)\nprint(energy.value)", answer: "min과 max로 상태의 유효 범위를 지킵니다." },
        debug: { title: "모든 객체가 값을 공유하는 버그", question: "클래스 속성 리스트를 객체별 인스턴스 속성으로 옮기세요.", codeTemplate: "class Bag:\n    items = []\n    def add(self, item):\n        self.items.append(item)\n\na = Bag()\nb = Bag()\na.add(\"검\")\nprint(b.items)", answer: "__init__ 안에 self.items = []를 선언해 객체마다 별도 리스트를 갖게 합니다." },
        creative: { title: "은행 계좌 시뮬레이터", question: "입금·출금·거래 내역 기능과 잔액 검사를 가진 클래스를 완성하세요.", codeTemplate: "class Account:\n    def __init__(self, owner):\n        self.owner = owner\n        self.balance = 0\n        self.history = []\n    def deposit(self, amount):\n        if amount > 0:\n            self.balance += amount\n            self.history.append((\"입금\", amount))\n    def withdraw(self, amount):\n        if 0 < amount <= self.balance:\n            self.balance -= amount\n            self.history.append((\"출금\", amount))\n\naccount = Account(\"민준\")\naccount.deposit(10000)\naccount.withdraw(3000)\nprint(account.balance, account.history)", answer: "잘못된 입금·잔액 초과 출금도 시험해 상태가 안전한지 확인합니다." },
        checkpoint: { question: "객체 상태를 바꾸기 전에 해야 할 일은?", options: ["항상 문자열 변환", "변경값과 현재 상태의 유효성 검사", "객체 삭제", "import 제거"], answer: 1, explanation: "객체가 허용하지 않는 상태가 되지 않도록 규칙을 검사합니다." }, homework: "아이템 수량이 음수가 되지 않는 재고 클래스를 만들기",
    },
    {
        number: 26, title: "상속으로 공통 기능을 확장해요", subtitle: "부모·자식 클래스, super(), 메서드 재정의를 익힙니다.", concept: "상속과 다형성", analogy: "공통 교복 규칙을 물려받되 동아리마다 배지와 활동을 다르게 추가하는 것과 같아요.", explanation: "여러 클래스의 공통 속성과 메서드를 부모에 두고 자식이 필요한 기능을 추가하거나 바꿀 수 있습니다.", syntax: ["class Mage(Character):\n    def __init__(self, name):\n        super().__init__(name, 80)", "def attack(self):\n    return \"마법 공격\""],
        predict: { code: "class Animal:\n    def sound(self):\n        return \"소리\"\nclass Cat(Animal):\n    def sound(self):\n        return \"야옹\"\nprint(Cat().sound())", question: "출력 결과는?", options: ["소리", "야옹", "Animal", "오류"], answer: 1, explanation: "자식 Cat이 sound 메서드를 재정의해 야옹을 반환합니다." },
        guided: { title: "캐릭터 직업 상속", question: "공통 Character를 상속한 Warrior와 Mage의 공격 문장을 다르게 만드세요.", codeTemplate: "class Character:\n    def __init__(self, name, hp):\n        self.name = name\n        self.hp = hp\n    def attack(self):\n        return \"기본 공격\"\n\nclass Warrior(Character):\n    def attack(self):\n        return \"검 공격\"\n\nclass Mage(Character):\n    def attack(self):\n        return \"마법 공격\"\n\nfor hero in [Warrior(\"루미\", 120), Mage(\"코코\", 80)]:\n    print(hero.name, hero.attack())", answer: "같은 attack 호출이 객체 종류에 따라 다른 결과를 냅니다." },
        check: { question: "부모 클래스의 초기화 메서드를 호출할 때 사용하는 것은?", options: ["parent()", "base()", "super()", "root()"], answer: 2, explanation: "super().__init__()으로 부모의 초기화 로직을 재사용합니다." },
        build: { title: "알림 메시지 다형성", question: "Email과 App 알림이 같은 send 메서드로 다른 문장을 반환하게 하세요.", codeTemplate: "class Notification:\n    def send(self, message):\n        return message\nclass EmailNotification(Notification):\n    def send(self, message):\n        return f\"이메일: {message}\"\nclass AppNotification(Notification):\n    def send(self, message):\n        return f\"앱 알림: {message}\"\n\nfor notice in [EmailNotification(), AppNotification()]:\n    print(notice.send(\"수업 시작\"))", answer: "공통 인터페이스 send를 객체마다 다르게 구현합니다." },
        debug: { title: "부모 초기화 누락", question: "name 속성이 생성되도록 자식 초기화에서 super를 호출하세요.", codeTemplate: "class Character:\n    def __init__(self, name):\n        self.name = name\nclass Mage(Character):\n    def __init__(self, name, mana):\n        self.mana = mana\n\nmage = Mage(\"코코\", 50)\nprint(mage.name)", answer: "Mage.__init__ 안에 super().__init__(name)을 추가합니다." },
        creative: { title: "몬스터 종류 확장", question: "Monster 부모와 Slime·Dragon 자식 클래스를 만들고 서로 다른 스킬을 구현하세요.", codeTemplate: "class Monster:\n    def __init__(self, name, hp):\n        self.name = name\n        self.hp = hp\n    def skill(self):\n        return \"몸통 박치기\"\nclass Slime(Monster):\n    def skill(self):\n        return \"점액 공격\"\nclass Dragon(Monster):\n    def skill(self):\n        return \"불꽃 숨결\"\n\nmonsters = [Slime(\"초록 슬라임\", 30), Dragon(\"붉은 용\", 200)]\nfor monster in monsters:\n    print(monster.name, monster.skill())", answer: "세 번째 몬스터 종류를 추가해 기존 반복 코드를 수정하지 않고 동작하게 합니다." },
        checkpoint: { question: "상속이 알맞은 상황은?", options: ["관련 없는 클래스 연결", "공통 특징을 가진 종류들을 확장", "변수 한 개 저장", "파일 한 줄 읽기"], answer: 1, explanation: "is-a 관계와 공통 동작이 있을 때 상속을 고려합니다." }, homework: "Vehicle 부모와 Bike·Car 자식 클래스로 이동 기능 확장하기",
    },
    {
        number: 27, title: "JSON으로 프로그램 데이터를 교환해요", subtitle: "딕셔너리와 JSON 문자열의 변환·검증을 익힙니다.", concept: "JSON", analogy: "JSON은 서로 다른 프로그램이 같은 양식으로 작성해 주고받는 표준 택배 상자예요.", explanation: "파이썬 자료구조를 텍스트 형식으로 저장하고 다른 프로그램과 교환할 수 있습니다.", syntax: ["text = json.dumps(data, ensure_ascii=False)", "data = json.loads(text)"],
        predict: { code: "import json\ntext = '{\"level\": 3, \"items\": [\"검\", \"물약\"]}'\ndata = json.loads(text)\nprint(data[\"items\"][1])", question: "출력 결과는?", options: ["검", "물약", "items", "3"], answer: 1, explanation: "JSON을 딕셔너리로 바꾼 뒤 items 리스트의 두 번째 값을 읽습니다." },
        guided: { title: "게임 저장 데이터 변환", question: "플레이어 딕셔너리를 JSON 문자열로 바꾸고 다시 복원하세요.", codeTemplate: "import json\nplayer = {\"name\": \"루미\", \"level\": 5, \"items\": [\"검\", \"물약\"]}\ntext = json.dumps(player, ensure_ascii=False)\nrestored = json.loads(text)\nprint(text)\nprint(restored[\"name\"], restored[\"level\"])", answer: "dumps는 문자열로 저장하고 loads는 파이썬 자료구조로 복원합니다." },
        check: { question: "JSON 문자열을 파이썬 자료구조로 바꾸는 함수는?", options: ["json.dumps", "json.loads", "json.print", "json.convert"], answer: 1, explanation: "loads의 s는 string을 읽는다는 의미로 기억할 수 있습니다." },
        build: { title: "설정 데이터 읽기", question: "JSON 설정에서 음량과 테마를 읽고 안전한 기본값도 사용하세요.", codeTemplate: "import json\ntext = '{\"volume\": 70, \"theme\": \"dark\"}'\nsettings = json.loads(text)\nprint(\"음량:\", settings.get(\"volume\", 50))\nprint(\"언어:\", settings.get(\"language\", \"ko\"))", answer: "필수 키가 없을 수 있으면 get()으로 기본값을 지정합니다." },
        debug: { title: "JSON 따옴표 오류", question: "JSON 표준에 맞게 키와 문자열 값을 큰따옴표로 고치세요.", codeTemplate: "import json\ntext = \"{'name': '루미', 'level': 3}\"\nprint(json.loads(text))", answer: "text = '{\"name\": \"루미\", \"level\": 3}'로 고칩니다." },
        creative: { title: "학습 기록 JSON", question: "수업명·점수·완료 활동을 JSON으로 만들고 요약 문장을 출력하세요.", codeTemplate: "import json\nrecord = {\"lesson\": \"Python Core 27\", \"score\": 3, \"completed\": [\"quiz\", \"code\", \"debug\"]}\ntext = json.dumps(record, ensure_ascii=False)\nloaded = json.loads(text)\nprint(loaded[\"lesson\"], len(loaded[\"completed\"]))", answer: "학습 시간을 추가하고 저장 전후 데이터가 같은지 비교합니다." },
        checkpoint: { question: "JSON이 특히 유용한 이유는?", options: ["파이썬에서만 사용", "프로그램 간 구조화된 데이터 교환", "이미지 편집", "코드 자동 삭제"], answer: 1, explanation: "많은 언어와 서비스가 공통으로 이해하는 텍스트 데이터 형식입니다." }, homework: "나의 프로필과 관심사를 JSON 문자열로 저장하고 복원하기",
    },
    {
        number: 28, title: "CSV 데이터를 읽고 요약해요", subtitle: "표 형태 데이터, csv 모듈, 합계·평균·필터를 연습합니다.", concept: "CSV 데이터 처리", analogy: "CSV는 표의 한 줄을 한 행으로, 쉼표를 열 경계로 저장하는 간단한 데이터 장부예요.", explanation: "행과 열로 된 데이터를 읽어 필요한 열을 숫자로 바꾸고 통계나 조건 필터를 적용합니다.", syntax: ["reader = csv.DictReader(file)", "scores = [int(row[\"score\"]) for row in reader]"],
        predict: { code: "import csv\nfrom io import StringIO\ndata = 'name,score\\n민준,80\\n서연,90'\nrows = list(csv.DictReader(StringIO(data)))\nprint(rows[1][\"score\"])", question: "출력되는 값은?", options: ["서연", "90", "score", "2"], answer: 1, explanation: "두 번째 행의 score 열 값인 문자열 90이 출력됩니다." },
        guided: { title: "점수 평균 분석", question: "CSV 문자열에서 점수 열을 읽어 평균을 계산하세요.", codeTemplate: "import csv\nfrom io import StringIO\ndata = 'name,score\\n민준,80\\n서연,90\\n지호,70'\nreader = csv.DictReader(StringIO(data))\nscores = [int(row[\"score\"]) for row in reader]\nprint(sum(scores) / len(scores))", answer: "CSV의 값은 문자열이므로 int로 변환한 뒤 계산합니다." },
        check: { question: "DictReader의 장점은?", options: ["열 이름으로 값 조회", "사진 자동 생성", "무조건 숫자 변환", "파일 암호화"], answer: 0, explanation: "첫 행의 열 이름을 딕셔너리 키처럼 사용할 수 있습니다." },
        build: { title: "기준 점수 통과자", question: "CSV 학생 데이터에서 80점 이상 이름만 출력하세요.", codeTemplate: "import csv\nfrom io import StringIO\ndata = 'name,score\\n민준,82\\n서연,76\\n지호,95'\nfor row in csv.DictReader(StringIO(data)):\n    if int(row[\"score\"]) >= 80:\n        print(row[\"name\"])", answer: "score 값을 정수로 변환해 기준과 비교합니다." },
        debug: { title: "문자열 합계 오류", question: "점수 문자열을 정수로 변환해 합계가 170이 되게 고치세요.", codeTemplate: "scores = [\"80\", \"90\"]\ntotal = sum(scores)\nprint(total)", answer: "total = sum(int(score) for score in scores)로 고칩니다." },
        creative: { title: "학급 성적 리포트", question: "평균·최고점·80점 이상 학생을 포함한 리포트를 출력하세요.", codeTemplate: "import csv\nfrom io import StringIO\ndata = 'name,score\\n민준,82\\n서연,76\\n지호,95\\n하린,88'\nrows = list(csv.DictReader(StringIO(data)))\nscores = [int(row[\"score\"]) for row in rows]\npassed = [row[\"name\"] for row in rows if int(row[\"score\"]) >= 80]\nprint(\"평균\", sum(scores) / len(scores))\nprint(\"최고\", max(scores))\nprint(\"통과\", passed)", answer: "최저점이나 등급 분포를 한 가지 추가하고 데이터가 바뀌어도 동작하게 합니다." },
        checkpoint: { question: "CSV 계산 전에 자주 필요한 처리로 알맞은 것은?", options: ["문자열 숫자를 int/float로 변환", "모든 쉼표 삭제", "클래스 상속", "이미지 크기 변경"], answer: 0, explanation: "CSV에서 읽은 값은 보통 문자열이므로 계산용 자료형으로 변환합니다." }, homework: "가상의 주간 공부 시간 CSV를 만들고 과목별 합계를 계산하기",
    },
    {
        number: 29, title: "재귀로 같은 구조의 작은 문제를 풀어요", subtitle: "기저 조건, 재귀 호출, 호출 흐름을 그림으로 이해합니다.", concept: "재귀 함수", analogy: "큰 상자 안의 작은 상자를 계속 열다가 마지막 상자에서 멈추는 구조예요.", explanation: "함수가 더 작은 같은 문제를 호출하고, 반드시 멈추는 기저 조건에서 값을 반환합니다.", syntax: ["def countdown(n):\n    if n == 0:\n        return\n    print(n)\n    countdown(n - 1)", "def factorial(n):\n    if n <= 1:\n        return 1\n    return n * factorial(n - 1)"],
        predict: { code: "def total(n):\n    if n == 0:\n        return 0\n    return n + total(n - 1)\nprint(total(3))", question: "출력 결과는?", options: ["3", "5", "6", "9"], answer: 2, explanation: "3 + 2 + 1 + 0으로 계산되어 6이 됩니다." },
        guided: { title: "재귀 카운트다운", question: "n부터 1까지 출력하고 0에서 멈추는 함수를 실행하세요.", codeTemplate: "def countdown(n):\n    if n == 0:\n        print(\"출발!\")\n        return\n    print(n)\n    countdown(n - 1)\n\ncountdown(5)", answer: "n == 0이 기저 조건이고 매 호출에서 n이 1씩 줄어듭니다." },
        check: { question: "재귀 함수에 반드시 필요한 것은?", options: ["무한 호출", "기저 조건", "파일 쓰기", "난수"], answer: 1, explanation: "기저 조건이 없으면 호출이 끝나지 않아 RecursionError가 납니다." },
        build: { title: "팩토리얼 계산", question: "n!을 재귀로 계산하고 5!을 출력하세요.", codeTemplate: "def factorial(n):\n    if n <= 1:\n        return 1\n    return n * factorial(n - 1)\n\nprint(factorial(5))", answer: "5 × 4 × 3 × 2 × 1 = 120이 됩니다." },
        debug: { title: "멈추지 않는 재귀", question: "호출할수록 n이 기저 조건에 가까워지도록 고치세요.", codeTemplate: "def countdown(n):\n    if n == 0:\n        return\n    print(n)\n    countdown(n + 1)\n\ncountdown(3)", answer: "countdown(n - 1)로 고칩니다." },
        creative: { title: "중첩 리스트 숫자 합계", question: "리스트 안에 리스트가 있는 자료의 모든 숫자를 재귀로 더하세요.", codeTemplate: "def deep_sum(data):\n    total = 0\n    for item in data:\n        if isinstance(item, list):\n            total += deep_sum(item)\n        else:\n            total += item\n    return total\n\nprint(deep_sum([1, [2, 3], [4, [5]]]))", answer: "중첩 깊이를 바꾼 데이터에서도 합계가 맞는지 확인합니다." },
        checkpoint: { question: "재귀 호출이 안전하게 끝나려면?", options: ["문제가 기저 조건에 가까워져야 함", "항상 값 증가", "return 삭제", "전역 변수 사용"], answer: 0, explanation: "매 호출에서 문제 크기가 줄어 기저 조건에 도달해야 합니다." }, homework: "1부터 n까지 합을 반복문과 재귀 두 방법으로 만들고 비교하기",
    },
    {
        number: 30, title: "스택과 큐로 처리 순서를 설계해요", subtitle: "LIFO·FIFO와 리스트·deque 활용을 익힙니다.", concept: "스택과 큐", analogy: "스택은 접시 더미, 큐는 놀이공원 대기 줄처럼 꺼내는 순서가 달라요.", explanation: "데이터를 넣고 꺼내는 규칙을 정해 뒤로 가기, 작업 대기열 같은 기능을 구현합니다.", syntax: ["stack.append(item)\nlast = stack.pop()", "queue.append(item)\nfirst = queue.popleft()"],
        predict: { code: "stack = []\nstack.append(\"A\")\nstack.append(\"B\")\nprint(stack.pop())", question: "출력 결과는?", options: ["A", "B", "AB", "오류"], answer: 1, explanation: "스택은 마지막에 넣은 B를 먼저 꺼냅니다." },
        guided: { title: "실행 취소 스택", question: "세 작업을 저장하고 마지막 작업을 취소하세요.", codeTemplate: "history = []\nfor action in [\"글자 입력\", \"색 변경\", \"크기 변경\"]:\n    history.append(action)\nundo = history.pop()\nprint(\"취소:\", undo)\nprint(\"남은 기록:\", history)", answer: "마지막 작업을 pop()으로 꺼내 실행 취소 흐름을 표현합니다." },
        check: { question: "먼저 들어온 작업을 먼저 처리하는 구조는?", options: ["스택", "큐", "세트", "튜플"], answer: 1, explanation: "큐는 FIFO, 즉 선입선출 구조입니다." },
        build: { title: "프린터 대기열", question: "deque에 문서를 넣고 앞에서부터 출력 처리하세요.", codeTemplate: "from collections import deque\nqueue = deque([\"수학.pdf\", \"코딩.pdf\", \"그림.pdf\"])\nwhile queue:\n    document = queue.popleft()\n    print(\"출력:\", document)", answer: "popleft()로 가장 먼저 들어온 문서부터 처리합니다." },
        debug: { title: "큐 순서 오류", question: "뒤에서 pop해 순서가 뒤집히는 코드를 앞에서 꺼내도록 고치세요.", codeTemplate: "from collections import deque\nqueue = deque([\"1번\", \"2번\", \"3번\"])\nwhile queue:\n    print(queue.pop())", answer: "queue.popleft()로 고칩니다." },
        creative: { title: "고객 지원 대기 시스템", question: "일반 고객은 뒤에, 긴급 고객은 앞에 추가해 처리 순서를 만드세요.", codeTemplate: "from collections import deque\nqueue = deque([\"일반A\", \"일반B\"])\nqueue.append(\"일반C\")\nqueue.appendleft(\"긴급X\")\nwhile queue:\n    print(\"상담:\", queue.popleft())", answer: "새 고객과 긴급 고객을 더 추가해 우선순위 규칙을 검증합니다." },
        checkpoint: { question: "브라우저의 뒤로 가기 기록에 잘 맞는 구조는?", options: ["스택", "큐", "세트", "숫자"], answer: 0, explanation: "가장 최근 방문 기록을 먼저 꺼내므로 스택이 어울립니다." }, homework: "할 일 대기열과 실행 취소 기록을 각각 큐와 스택으로 표현하기",
    },
    {
        number: 31, title: "테스트로 코드의 약속을 확인해요", subtitle: "assert, 정상·경계·예외 사례와 회귀 테스트를 익힙니다.", concept: "자동 테스트", analogy: "테스트는 제품을 출고하기 전 같은 기준으로 반복 검사하는 품질 검사표예요.", explanation: "기대 결과를 코드로 적어 수정 후에도 기존 기능이 망가지지 않았는지 빠르게 확인합니다.", syntax: ["assert add(2, 3) == 5", "assert grade(90) == \"A\""],
        predict: { code: "def double(n):\n    return n * 2\nassert double(3) == 6\nprint(\"통과\")", question: "출력 결과는?", options: ["3", "6", "통과", "AssertionError"], answer: 2, explanation: "assert 조건이 참이므로 다음 줄의 통과가 출력됩니다." },
        guided: { title: "계산 함수 테스트", question: "add 함수에 정상값·0·음수 테스트를 작성하세요.", codeTemplate: "def add(a, b):\n    return a + b\n\nassert add(2, 3) == 5\nassert add(0, 7) == 7\nassert add(-2, 2) == 0\nprint(\"모든 테스트 통과\")", answer: "서로 다른 종류의 입력 사례로 함수의 약속을 확인합니다." },
        check: { question: "경계값 테스트의 예는?", options: ["항상 같은 중간 값", "통과 기준 바로 아래·같음·바로 위", "화면 색상만 확인", "코드 길이 측정"], answer: 1, explanation: "조건이 바뀌는 경계 주변 값에서 오류가 자주 발생합니다." },
        build: { title: "등급 함수 경계 테스트", question: "90점 A, 89점 B가 되는 함수를 테스트하세요.", codeTemplate: "def grade(score):\n    if score >= 90:\n        return \"A\"\n    if score >= 80:\n        return \"B\"\n    return \"C\"\n\nassert grade(90) == \"A\"\nassert grade(89) == \"B\"\nassert grade(80) == \"B\"\nassert grade(79) == \"C\"\nprint(\"경계 테스트 통과\")", answer: "기준 바로 아래와 기준값을 모두 시험합니다." },
        debug: { title: "테스트가 찾아낸 조건 오류", question: "90점 테스트가 통과하도록 비교 연산자를 고치세요.", codeTemplate: "def is_pass(score):\n    return score > 70\n\nassert is_pass(70) is True", answer: "요구사항이 70점 이상 통과라면 return score >= 70으로 고칩니다." },
        creative: { title: "비밀번호 규칙과 테스트", question: "길이 8 이상·숫자 포함 규칙 함수와 다양한 테스트를 작성하세요.", codeTemplate: "def valid_password(password):\n    has_number = any(ch.isdigit() for ch in password)\n    return len(password) >= 8 and has_number\n\nassert valid_password(\"coding12\") is True\nassert valid_password(\"coding\") is False\nassert valid_password(\"12345678\") is True\nprint(\"규칙 테스트 통과\")", answer: "너무 짧음·숫자 없음·경계 길이·정상 사례를 모두 포함합니다." },
        checkpoint: { question: "회귀 테스트의 목적은?", options: ["이전 기능이 수정 후에도 유지되는지 확인", "코드 삭제", "난수 생성", "파일 압축"], answer: 0, explanation: "새 변경이 이미 잘 되던 기능을 망가뜨리지 않았는지 확인합니다." }, homework: "이전에 만든 할인 함수에 다섯 가지 테스트 사례 추가하기",
    },
    {
        number: 32, title: "리팩터링으로 같은 기능을 더 읽기 좋게 만들어요", subtitle: "중복 제거, 함수 분리, 이름 개선과 작은 단계 수정을 익힙니다.", concept: "리팩터링", analogy: "방의 물건은 그대로 두되 찾기 쉽도록 분류하고 수납 위치를 바꾸는 정리 작업이에요.", explanation: "외부 동작은 유지하면서 코드 구조와 이름을 개선해 읽기·수정·테스트를 쉽게 만듭니다.", syntax: ["def calculate_total(price, count):\n    return price * count", "def format_receipt(menu, total):\n    return f\"{menu}: {total}원\""],
        predict: { code: "def area(width, height):\n    return width * height\nold_result = 4 * 5\nnew_result = area(4, 5)\nprint(old_result == new_result)", question: "출력 결과는?", options: ["True", "False", "20", "오류"], answer: 0, explanation: "구조는 바뀌었지만 계산 결과는 같아야 합니다." },
        guided: { title: "중복 계산 함수로 묶기", question: "반복되는 할인 계산을 함수 하나로 바꾸세요.", codeTemplate: "def sale_price(price, rate):\n    return int(price * (100 - rate) / 100)\n\nprint(sale_price(10000, 10))\nprint(sale_price(20000, 20))", answer: "달라지는 값을 매개변수로 만들고 공통 계산은 함수에 둡니다." },
        check: { question: "리팩터링 중 반드시 지켜야 할 것은?", options: ["외부 동작과 테스트 결과 유지", "모든 함수 삭제", "코드를 한 줄로 축약", "변수 이름 짧게"], answer: 0, explanation: "기능은 유지하고 내부 구조를 개선합니다." },
        build: { title: "영수증 기능 분리", question: "계산과 출력 형식을 서로 다른 함수로 분리하세요.", codeTemplate: "def calculate_total(price, count):\n    return price * count\n\ndef format_receipt(menu, total):\n    return f\"{menu}: {total}원\"\n\ntotal = calculate_total(4000, 2)\nprint(format_receipt(\"떡볶이\", total))", answer: "각 함수가 한 가지 책임만 맡도록 나눕니다." },
        debug: { title: "이름 때문에 생긴 이해 오류", question: "a, b, c를 역할이 드러나는 이름으로 바꾸세요.", codeTemplate: "def f(a, b):\n    c = a * b\n    return c\nprint(f(1500, 3))", answer: "def calculate_total(price, count): total = price * count처럼 고칩니다." },
        creative: { title: "퀴즈 프로젝트 리팩터링", question: "문제 출력·답 비교·점수 등급을 각각 함수로 나누세요.", codeTemplate: "def is_correct(answer, correct):\n    return answer == correct\n\ndef grade(score, total):\n    rate = score / total\n    return \"최고\" if rate >= 0.8 else \"도전\"\n\nanswers = [1, 2, 3]\ncorrect = [1, 4, 3]\nscore = sum(is_correct(a, c) for a, c in zip(answers, correct))\nprint(score, grade(score, len(correct)))", answer: "기존 결과를 테스트로 고정하고 함수 하나씩 분리합니다." },
        checkpoint: { question: "좋은 리팩터링 순서는?", options: ["테스트 확인→작게 수정→다시 테스트", "전부 삭제→새로 시작", "오류 숨기기", "한 번에 구조 전체 변경"], answer: 0, explanation: "작은 변경과 반복 검증이 안전합니다." }, homework: "내 과거 코드 하나에서 중복·이름·긴 함수 중 두 가지 개선하기",
    },
    {
        number: 33, title: "이진 탐색으로 범위를 절반씩 줄여요", subtitle: "정렬 조건, left·right·mid와 탐색 불변식을 익힙니다.", concept: "이진 탐색", analogy: "사전에서 가운데를 펴고 목표 단어가 앞인지 뒤인지 판단해 범위를 반으로 줄이는 방법이에요.", explanation: "정렬된 데이터에서 중간값과 목표를 비교해 탐색 범위를 매번 절반으로 줄입니다.", syntax: ["mid = (left + right) // 2", "if numbers[mid] < target:\n    left = mid + 1\nelse:\n    right = mid - 1"],
        predict: { code: "numbers = [10, 20, 30, 40, 50]\nleft, right = 0, 4\nmid = (left + right) // 2\nprint(numbers[mid])", question: "첫 번째 중간값은?", options: ["10", "20", "30", "40"], answer: 2, explanation: "중간 인덱스는 2이고 값은 30입니다." },
        guided: { title: "이진 탐색 함수", question: "정렬된 리스트에서 목표 값의 위치를 반환하세요.", codeTemplate: "def binary_search(numbers, target):\n    left, right = 0, len(numbers) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if numbers[mid] == target:\n            return mid\n        if numbers[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n\nprint(binary_search([10, 20, 30, 40, 50], 40))", answer: "목표 40의 위치 3을 반환합니다." },
        check: { question: "이진 탐색을 사용하기 위한 핵심 조건은?", options: ["데이터가 정렬됨", "문자열만 포함", "중복 없음", "길이 10 이하"], answer: 0, explanation: "중간값으로 앞뒤 방향을 판단하려면 정렬되어 있어야 합니다." },
        build: { title: "업다운 게임 판정", question: "정답과 추측을 비교해 범위를 줄이는 과정을 출력하세요.", codeTemplate: "answer = 73\nleft, right = 1, 100\nfor guess in [50, 75, 62, 68, 71, 73]:\n    if guess < answer:\n        left = guess + 1\n        print(guess, \"UP\", left, right)\n    elif guess > answer:\n        right = guess - 1\n        print(guess, \"DOWN\", left, right)\n    else:\n        print(guess, \"정답\")\n        break", answer: "각 비교 뒤 정답이 남아 있는 범위가 올바르게 줄어드는지 확인합니다." },
        debug: { title: "경계 갱신 버그", question: "같은 mid를 반복하지 않도록 left와 right를 한 칸 넘겨 갱신하세요.", codeTemplate: "numbers = [1, 3, 5, 7, 9]\ntarget = 8\nleft, right = 0, len(numbers) - 1\nwhile left <= right:\n    mid = (left + right) // 2\n    if numbers[mid] < target:\n        left = mid\n    else:\n        right = mid", answer: "left = mid + 1, right = mid - 1로 고칩니다." },
        creative: { title: "단어 사전 검색", question: "정렬된 단어 목록에서 목표 단어를 이진 탐색으로 찾으세요.", codeTemplate: "def binary_search(words, target):\n    left, right = 0, len(words) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if words[mid] == target:\n            return mid\n        if words[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n\nwords = sorted([\"python\", \"ai\", \"robot\", \"code\", \"game\"])\nprint(words, binary_search(words, \"robot\"))", answer: "목록에 없는 단어와 처음·마지막 단어도 시험합니다." },
        checkpoint: { question: "이진 탐색이 범위를 줄이는 방식은?", options: ["한 칸씩", "절반씩", "무작위", "두 배씩 늘림"], answer: 1, explanation: "중간값 비교로 남길 절반을 선택합니다." }, homework: "1~100 숫자 맞히기를 사람이 아닌 컴퓨터가 이진 탐색으로 수행하게 만들기",
    },
    {
        number: 34, title: "문제 해결 패턴으로 낯선 문제를 분해해요", subtitle: "빈도 세기, 최댓값 추적, 투 포인터 기초를 연습합니다.", concept: "문제 해결 패턴", analogy: "요리마다 공통 조리법이 있듯 코딩 문제에도 반복해서 쓰이는 해결 틀이 있어요.", explanation: "문제를 읽고 입력·출력·제약을 정리한 뒤 알맞은 패턴을 선택하고 작은 사례로 검증합니다.", syntax: ["counts[value] = counts.get(value, 0) + 1", "best = max(items, key=lambda item: item[\"score\"])"],
        predict: { code: "text = \"banana\"\ncounts = {}\nfor ch in text:\n    counts[ch] = counts.get(ch, 0) + 1\nprint(counts[\"a\"])", question: "a의 빈도는?", options: ["1", "2", "3", "4"], answer: 2, explanation: "banana에는 a가 세 번 등장합니다." },
        guided: { title: "문자 빈도표", question: "문장의 공백을 제외하고 각 글자의 등장 횟수를 세세요.", codeTemplate: "text = \"hello python\"\ncounts = {}\nfor ch in text:\n    if ch != \" \":\n        counts[ch] = counts.get(ch, 0) + 1\nprint(counts)", answer: "딕셔너리 get()으로 처음 등장한 글자는 0에서 시작합니다." },
        check: { question: "가장 자주 등장한 값을 찾는 문제에 먼저 필요한 것은?", options: ["빈도 세기", "파일 삭제", "상속", "재귀만 사용"], answer: 0, explanation: "각 값의 등장 횟수를 계산한 뒤 최댓값을 찾습니다." },
        build: { title: "최고 점수 추적", question: "반복문으로 최고 점수 학생을 추적하세요.", codeTemplate: "students = [{\"name\": \"민준\", \"score\": 82}, {\"name\": \"서연\", \"score\": 95}, {\"name\": \"지호\", \"score\": 88}]\nbest = students[0]\nfor student in students[1:]:\n    if student[\"score\"] > best[\"score\"]:\n        best = student\nprint(best)", answer: "현재 최고 후보를 저장하고 더 큰 값을 만나면 교체합니다." },
        debug: { title: "최댓값 초기화 오류", question: "모두 음수인 데이터에서도 올바른 최댓값을 찾도록 초기값을 고치세요.", codeTemplate: "numbers = [-8, -3, -12]\nbest = 0\nfor number in numbers:\n    if number > best:\n        best = number\nprint(best)", answer: "best = numbers[0]으로 실제 첫 값에서 시작합니다." },
        creative: { title: "가장 인기 있는 메뉴", question: "주문 리스트의 빈도를 세고 가장 많이 주문된 메뉴를 찾으세요.", codeTemplate: "orders = [\"김밥\", \"라면\", \"김밥\", \"떡볶이\", \"김밥\", \"라면\"]\ncounts = {}\nfor menu in orders:\n    counts[menu] = counts.get(menu, 0) + 1\npopular = max(counts, key=counts.get)\nprint(popular, counts[popular])", answer: "동률일 때 어떤 규칙을 사용할지 정하고 코드에 표현합니다." },
        checkpoint: { question: "문제를 분해할 때 가장 먼저 확인할 것은?", options: ["입력·원하는 출력·제약", "글꼴", "파일 이름", "코드 줄 수"], answer: 0, explanation: "무엇이 주어지고 무엇을 만들어야 하며 제한이 무엇인지 정리합니다." }, homework: "생활 속 빈도 세기 문제 하나를 정해 데이터와 결과를 직접 만들기",
    },
    {
        number: 35, title: "최종 프로젝트를 기획하고 프로토타입을 만들어요", subtitle: "문제 정의, 사용자 흐름, 데이터·기능 설계와 MVP를 완성합니다.", concept: "프로젝트 기획과 MVP", analogy: "큰 건물을 짓기 전에 사용 목적과 동선을 정하고 작은 모형부터 만드는 과정이에요.", explanation: "누구의 어떤 문제를 해결할지 정하고 꼭 필요한 기능만 담은 최소 동작 버전을 먼저 만듭니다.", syntax: ["features = [\"추가\", \"조회\", \"완료\"]", "data = [{\"title\": \"파이썬 복습\", \"done\": False}]"],
        predict: { code: "features = [\"추가\", \"조회\", \"완료\", \"통계\"]\nmvp = features[:2]\nprint(mvp)", question: "MVP에 먼저 포함된 기능은?", options: ["추가·조회", "완료·통계", "추가·통계", "모든 기능"], answer: 0, explanation: "슬라이싱으로 앞의 핵심 기능 두 개만 선택합니다." },
        guided: { title: "문제와 사용자 정의", question: "프로젝트의 사용자·문제·핵심 기능을 딕셔너리로 정리하세요.", codeTemplate: "plan = {\n    \"user\": \"공부 계획을 자주 잊는 학생\",\n    \"problem\": \"해야 할 일을 한눈에 보기 어렵다\",\n    \"features\": [\"할 일 추가\", \"목록 보기\", \"완료 처리\"]\n}\nfor key, value in plan.items():\n    print(key, value)", answer: "사용자와 문제가 구체적이고 기능이 문제 해결과 연결되어야 합니다." },
        check: { question: "MVP의 의미로 가장 알맞은 것은?", options: ["가능한 기능을 모두 넣은 완성품", "핵심 가치를 확인할 최소 동작 제품", "화면만 있는 그림", "오류가 있는 코드"], answer: 1, explanation: "가장 중요한 사용 흐름이 실제로 동작하는 최소 버전입니다." },
        build: { title: "할 일 앱 핵심 데이터", question: "할 일을 추가하고 완료 상태를 변경하는 함수를 만드세요.", codeTemplate: "tasks = []\ndef add_task(title):\n    tasks.append({\"title\": title, \"done\": False})\ndef complete_task(index):\n    if 0 <= index < len(tasks):\n        tasks[index][\"done\"] = True\n\nadd_task(\"파이썬 복습\")\nadd_task(\"프로젝트 기획\")\ncomplete_task(0)\nprint(tasks)", answer: "추가·완료라는 핵심 기능이 데이터 상태를 정확히 바꿉니다." },
        debug: { title: "요구사항과 코드 불일치", question: "완료 기능이 제목을 삭제하지 않고 done 상태만 바꾸도록 고치세요.", codeTemplate: "tasks = [{\"title\": \"복습\", \"done\": False}]\ndef complete_task(index):\n    tasks.pop(index)\ncomplete_task(0)\nprint(tasks)", answer: "tasks[index][\"done\"] = True로 상태만 변경합니다." },
        creative: { title: "나의 캡스톤 MVP", question: "학습관리·게임·생활도구 중 하나를 골라 핵심 데이터와 기능 3개를 구현하세요.", codeTemplate: "records = []\ndef add_record(title, minutes):\n    records.append({\"title\": title, \"minutes\": minutes})\ndef total_minutes():\n    return sum(record[\"minutes\"] for record in records)\n\nadd_record(\"파이썬\", 40)\nadd_record(\"알고리즘\", 30)\nprint(records)\nprint(\"총 학습:\", total_minutes())", answer: "사용자 문제·데이터 구조·핵심 함수 3개·예시 실행이 포함되어야 합니다." },
        checkpoint: { question: "프로젝트 범위가 너무 클 때 가장 좋은 결정은?", options: ["모든 기능을 동시에 시작", "핵심 사용자 흐름을 남기고 나머지는 이후로 미룸", "테스트 삭제", "목표 없이 코딩"], answer: 1, explanation: "핵심 가치를 검증하는 기능부터 완성합니다." }, homework: "캡스톤 기획서와 핵심 기능 세 개의 실행 가능한 프로토타입 준비하기",
    },
    {
        number: 36, title: "최종 프로젝트를 완성하고 설명해요", subtitle: "통합 테스트, 오류 수정, 사용 설명, 발표와 자기평가로 과정을 마무리합니다.", concept: "통합·검증·발표", analogy: "각 부품을 만든 뒤 실제 사용 순서대로 전체 제품을 시험하고 사용법을 소개하는 단계예요.", explanation: "기능을 연결하고 정상·경계·오류 사례를 테스트한 뒤 다른 사람이 사용할 수 있도록 설명합니다.", syntax: ["def run_demo():\n    add_record(\"파이썬\", 40)\n    assert total_minutes() == 40", "print(\"문제 → 해결 방법 → 핵심 코드 → 개선 계획\")"],
        predict: { code: "def safe_average(numbers):\n    return sum(numbers) / len(numbers) if numbers else 0\nchecks = [safe_average([10, 20]) == 15, safe_average([]) == 0]\nprint(all(checks))", question: "모든 테스트 결과는?", options: ["True", "False", "15", "0"], answer: 0, explanation: "정상 리스트와 빈 리스트 테스트가 모두 참이라 all 결과는 True입니다." },
        guided: { title: "통합 테스트 시나리오", question: "추가·완료·통계 흐름을 한 번에 실행하고 assert로 검증하세요.", codeTemplate: "tasks = []\ndef add_task(title):\n    tasks.append({\"title\": title, \"done\": False})\ndef complete_task(index):\n    tasks[index][\"done\"] = True\ndef completed_count():\n    return sum(task[\"done\"] for task in tasks)\n\nadd_task(\"복습\")\nadd_task(\"발표\")\ncomplete_task(0)\nassert len(tasks) == 2\nassert completed_count() == 1\nprint(\"통합 테스트 통과\")", answer: "사용자가 실제로 사용하는 순서대로 여러 기능을 연결해 확인합니다." },
        check: { question: "좋은 프로젝트 발표에 포함할 내용은?", options: ["코드 전체 낭독", "해결한 문제·핵심 기능·시연·개선 계획", "오류 숨기기", "파일 이름만 소개"], answer: 1, explanation: "무엇을 왜 만들었고 어떻게 동작하며 무엇을 더 개선할지 전달합니다." },
        build: { title: "사용 설명과 오류 안내", question: "잘못된 위치를 입력해도 프로그램이 멈추지 않는 완료 함수를 만드세요.", codeTemplate: "tasks = [{\"title\": \"복습\", \"done\": False}]\ndef complete_task(index):\n    if not 0 <= index < len(tasks):\n        return \"존재하지 않는 번호입니다\"\n    tasks[index][\"done\"] = True\n    return \"완료 처리했습니다\"\n\nprint(complete_task(0))\nprint(complete_task(5))", answer: "정상 입력과 잘못된 입력 모두 사용자에게 이해 가능한 결과를 반환합니다." },
        debug: { title: "통합 후 생긴 데이터 키 오류", question: "프로젝트 전체에서 사용하는 키 이름을 title로 통일하세요.", codeTemplate: "tasks = [{\"title\": \"복습\", \"done\": True}]\nfor task in tasks:\n    print(task[\"name\"], task[\"done\"])", answer: "task[\"name\"]을 task[\"title\"]로 고칩니다." },
        creative: { title: "캡스톤 최종 시연", question: "나의 프로젝트 핵심 기능과 테스트, 한계와 다음 개선 기능을 출력 가능한 형태로 정리하세요.", codeTemplate: "project = {\n    \"title\": \"나의 학습 기록\",\n    \"problem\": \"공부 시간을 기억하기 어렵다\",\n    \"features\": [\"기록 추가\", \"합계\", \"목표 확인\"],\n    \"next\": \"파일 저장 기능\"\n}\nprint(\"프로젝트:\", project[\"title\"])\nprint(\"해결 문제:\", project[\"problem\"])\nfor feature in project[\"features\"]:\n    print(\"-\", feature)\nprint(\"다음 개선:\", project[\"next\"])", answer: "실제 프로젝트 실행 결과와 테스트 증거를 보여주고 질문에 핵심 코드로 답합니다." },
        checkpoint: { question: "완성된 프로젝트의 마지막 검증으로 가장 알맞은 것은?", options: ["내 컴퓨터에서 한 번 실행", "다른 사용자가 설명 없이 사용해 보고 피드백 제공", "오류 메시지 삭제", "코드 줄 수 늘리기"], answer: 1, explanation: "실제 사용자 관찰은 놓친 사용성 문제와 요구사항을 찾게 합니다." }, homework: "최종 프로젝트 발표 영상 또는 1쪽 결과 보고서와 다음 개선 계획 작성하기",
    },
] satisfies PythonCoreSessionSpec[];
